# Expo/React Native tooling (Babel, Metro) assumes a flat node_modules and
# resolves transitive deps like babel-preset-expo from the top level. pnpm's
# default isolated linker hides those, which breaks EAS builds
# ("Cannot find module 'babel-preset-expo'"). Use a flat layout instead.
node-linker=hoisted
